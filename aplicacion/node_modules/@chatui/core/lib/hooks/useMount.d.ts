import React from 'react';
declare type useMountProps = {
    active?: boolean;
    ref: React.RefObject<any>;
    delay?: number;
};
declare function useMount({ active, ref, delay }: useMountProps): {
    didMount: boolean;
    isShow: boolean;
};
export default useMount;
